/*

  unscan.js - a backbone-based framework for transcribing images to htlx.json.

  Inputs:
  
    media/
      directory containing image files (probably .jpg  or .png)


  Outputs:
  
    A data structure, in htlx JSON format.

*/


app = {};

app. = Backbone.View.extend({



})

app.Scan = Backbone.Model.extend({

})

app.Scans = Backbone.Collection.extend({

})
